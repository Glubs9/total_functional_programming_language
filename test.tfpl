catch{!} = destroy_self();
catch{a} = a;

ack_call{a,b} = ack{a,s[s[0]]};
ack_print(a,b) = print(ack_call{a,b});

zero() = 0;
print_ack(0) = zero();
print_ack(s[a]) = ack_print(a, print_ack(a));

make_list(a,b) = list[a,b];

data btree = branch[a,btree,btree] | leaf[a];

dfs(leaf[a]) = a;
dfs(branch[a,t1,t2]) = dfs(t1);

main{} = car(cdr(make_list(s[s[s[s[0]]]], make_list(s[s[s[0]]], s[s[0]]))));
