catch{!} = destroy_self();
catch{a} = a;

ack_call{a,b} = ack{a,s[s[0]]};
ack_print(a,b) = print(ack_call{a,b});

zero() = 0;
print_ack(0) = zero();
print_ack(s[a]) = ack_print(a, print_ack(a));

car(list[a,b]) = a;
cdr(list[a,b]) = b;
cons(a,b) = list[a,b];

main{} = cdr(cons(s[s[s[0]]], s[s[0]]));
